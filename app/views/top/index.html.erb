<div class="top-page">
	<div class="out-m">
	<% if flash[:notice] %>
		  <div class="flash-create">
		    <%= flash[:notice] %>
		  </div>
		<% end %>
	</div>
	<div class="top-content">
		<div class="top-headline">
		<h1>welcome to <span class="glyphicon glyphicon-book" aria-hidden="true"></span>Bookers  !!</h1>
		<p>In <span class="glyphicon glyphicon-book" aria-hidden="true"></span>Bookers,<br>
		you can share and exchange your opinions, inpressions, and emotions<br>
		about varioius books!
	    </p>
	    </div>
	    <% if user_signed_in? %>
		    <%= link_to "Log in", user_path(current_user.id), class:"btn btn-info btn-block active" %>
		    <%= link_to "Sign up", user_path(current_user.id), class:"btn btn-success btn-block active" %>
	    <% else %>
	        <%= link_to "Log in", "/users/sign_in", class:"btn btn-info btn-block active" %>
		    <%= link_to "Sign up", "/users/sign_up", class:"btn btn-success btn-block active" %>
		<% end %>
	    <p class="copy-right">CopyRight Infratop.inc</p>
	</div>
</div>